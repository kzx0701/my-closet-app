# 接口总览

## 说明

本文档用于说明当前项目中已经接入的接口能力，区分哪些是官方提供的通用能力，哪些是项目自研的业务接口。

## 一、官方提供的接口能力

### 1. `uni-id-pages`

- 来源：`uni_modules/uni-id-pages`
- 作用：提供登录、注册、找回密码、用户资料等官方页面能力
- 当前用法：
  - 登录页：`/uni_modules/uni-id-pages/pages/login/login-withpwd`
  - 注册页：`/uni_modules/uni-id-pages/pages/register/register`
  - 用户资料页：`/uni_modules/uni-id-pages/pages/userinfo/userinfo`

### 2. `uni-id-co`

- 来源：`uni_modules/uni-id-pages/uniCloud/cloudfunctions/uni-id-co`
- 作用：提供认证相关云对象能力
- 当前项目主要通过 `uni-id-pages` 间接使用这些能力
- 覆盖范围包括：
  - 登录
  - 注册
  - 退出登录
  - 获取用户信息
  - 验证 token

### 3. `uni-id-common`

- 来源：`uni_modules/uni-id-common`
- 作用：提供服务端鉴权与 token 校验基础能力
- 当前项目用途：
  - 在 `family-co` 的 `_before` 中校验当前请求 token

### 4. `uniCloud.importObject`

- 类型：uniCloud 官方客户端能力
- 当前封装文件：`common/api/clients/cloud-object.js`
- 用途：统一创建云对象调用实例

### 5. `uniCloud.database`

- 类型：uniCloud 官方数据库能力
- 当前封装文件：`common/api/clients/client-db.js`
- 用途：统一创建数据库实例和 collection 实例

## 二、项目自研业务接口

### 1. `family-co.getCurrentFamily`

- 前端入口：`common/api/modules/family.js -> fetchCurrentFamily()`
- 云端实现：`uniCloud-aliyun/cloudfunctions/family-co/index.obj.js`
- 作用：查询当前登录用户是否已加入家庭
- 典型使用场景：
  - 登录后统一分流
  - 首页判断当前是个人空间还是家庭空间

### 2. `family-co.createFamily`

- 前端入口：`common/api/modules/family.js -> createFamily(payload)`
- 云端实现：`uniCloud-aliyun/cloudfunctions/family-co/index.obj.js`
- 作用：创建新家庭，并自动把当前用户设置为管理员
- 典型使用场景：
  - 家庭引导页中的“创建家庭”流程

### 3. `family-co.joinFamilyByInviteCode`

- 前端入口：`common/api/modules/family.js -> joinFamilyByInviteCode(payload)`
- 云端实现：`uniCloud-aliyun/cloudfunctions/family-co/index.obj.js`
- 作用：通过邀请码加入已有家庭
- 典型使用场景：
  - 家庭引导页中的“加入家庭”流程

## 三、服务层说明

以下文件不是直接对外暴露的接口，而是页面和接口之间的业务编排层：

### 1. `common/services/auth.js`

- 作用：读取当前登录会话信息

### 2. `common/services/family-membership.js`

- 作用：在 `fetchCurrentFamily()` 基础上补充超时保护、错误分类和统一返回结构

### 3. `common/services/session-router.js`

- 作用：统一处理登录后的页面分流

### 4. `common/services/family-guide-state.js`

- 作用：在本地存储中维护“是否跳过家庭引导”的状态

## 四、当前接口现状

目前项目的接口结构非常清晰：

- 认证体系：官方提供
  - `uni-id-pages`
  - `uni-id-co`
  - `uni-id-common`

- 家庭业务：项目自研
  - `getCurrentFamily`
  - `createFamily`
  - `joinFamilyByInviteCode`

- 页面分流：项目自研服务层编排
  - `auth`
  - `family-membership`
  - `session-router`
  - `family-guide-state`

# 家庭衣橱 App 第一版开发流程

## 开发目标

第一版先跑通这条核心业务链路：

`注册登录 -> 创建家庭 / 加入家庭 -> 创建衣橱 -> 添加衣物 -> 查看与管理衣物`

## 当前已确认规则

- 一个用户只能加入一个家庭
- 一个家庭可以有多个衣橱
- 一件衣物可以不属于任何衣橱
- 家庭成员都可以创建衣物
- 普通成员只能管理自己创建的衣物
- 管理员可以管理家庭中所有成员创建的衣物

## 开发原则

- 先定数据结构，再做页面和接口
- 先跑通最小业务闭环，再补交互细节
- 第一版只做核心功能，不提前扩展复杂能力
- 需求变更后，及时同步更新需求文档和本流程文档

## 开发步骤

## 1. 明确数据模型

先确认第一版的核心对象和关系：

- 用户
- 家庭
- 家庭成员关系
- 衣橱
- 衣物

这一阶段重点确认：

- 哪些数据属于用户
- 哪些数据属于家庭
- 哪些数据允许为空，例如衣物的 `closet_id`
- 哪些操作受角色权限控制

## 2. 建立数据库表

在 `uniCloud` 中先建立第一版必需的数据表：

- `uni-id-users`
- `families`
- `family_members`
- `closets`
- `clothes`

同时确定：

- 字段名
- 字段类型
- 表间关联
- 索引
- 读写权限规则

## 3. 接入注册登录

使用 `uni-id-pages` 完成：

- 注册
- 登录
- 获取当前用户信息
- 退出登录

这一步完成后，应用要能识别当前用户身份，为后续家庭和衣物操作提供用户上下文。

## 4. 开发家庭流程

先开发家庭相关的最小闭环：

- 创建家庭
- 生成家庭邀请码
- 通过邀请码加入家庭
- 查询当前用户所属家庭
- 查询家庭成员列表

同时需要保证：

- 一个用户只能加入一个家庭
- 已加入家庭的用户不能再次加入其他家庭

### 注册登录如何处理

第一版不建议自己重写登录注册。

建议直接复用：

- `uni-id-pages` 的前端页面能力
- `uni-id-co` 的云对象能力
- `uni-id-users` 的用户表

这里的关系要明确：

- `uni-id-pages` 不是数据表
- `uni-id-pages` 是官方提供的一套登录注册页面和调用封装
- `uni-id-co` 是它配套的云对象
- `uni-id-users` 才是真正存用户数据的表

第一版建议只启用最简单的登录方式：

- 用户名密码注册
- 用户名密码登录

短信验证码、一键登录、第三方登录都先不开。

### 创建家庭如何处理

创建家庭建议不要直接由前端写数据库。

建议通过自定义云对象或云函数统一处理，原因是这里有明显的业务校验：

- 用户是否已加入家庭
- 邀请码是否唯一
- 创建家庭后是否自动写入成员表
- 创建人是否自动设为管理员

建议第一版优先设计以下接口：

- `createFamily`
- `joinFamilyByInviteCode`
- `getCurrentFamily`
- `getFamilyMembers`

其中：

- `createFamily` 负责创建 `families` 和 `family_members`
- `joinFamilyByInviteCode` 负责校验邀请码并写入成员关系
- `getCurrentFamily` 负责查询当前用户所属家庭
- `getFamilyMembers` 负责查询当前家庭成员列表

## 当前是否要删除其他表和插件

不建议现在直接删除 `uni_modules` 里的插件资源文件和 schema 文件。

原因：

- 这些文件大多属于插件自带资源，不初始化数据库并不会影响当前业务
- 直接删除后，后续如果继续用 `uni-id-pages` 登录能力，容易破坏依赖
- 当前真正需要控制的是“初始化哪些表”，而不是“物理删除哪些插件文件”

所以当前推荐做法是：

- 不初始化不用的表
- 保留插件目录文件
- 仅初始化第一版真正需要的数据库表

## 5. 开发衣橱流程

在家庭维度下开发衣橱管理：

- 创建衣橱
- 查询衣橱列表
- 编辑衣橱
- 删除衣橱

这里要注意：

- 衣橱归属于家庭
- 一个家庭可以有多个衣橱
- 删除衣橱时，要考虑已绑定该衣橱的衣物如何处理

第一版建议处理方式：

- 删除衣橱时，将关联衣物的 `closet_id` 置空

## 6. 开发衣物管理

这是第一版最核心的模块，优先完成：

- 添加衣物
- 查看衣物列表
- 查看衣物详情
- 编辑衣物
- 删除衣物
- 为衣物选择所属衣橱
- 支持衣物不绑定衣橱

同时实现基础筛选：

- 按衣橱筛选
- 按创建人筛选
- 按分类筛选
- 按季节筛选

## 7. 开发权限控制

在衣物和衣橱模块开发时，同步完成权限规则：

- 普通成员只能编辑和删除自己创建的衣物
- 管理员可以编辑和删除家庭内所有衣物
- 非本家庭成员不能读取家庭数据

建议：

- 简单读取可用 `clientDB`
- 涉及权限判断的创建、编辑、删除优先走云函数或云对象

## 8. 搭建页面结构

在业务接口基本明确后，搭建页面：

- 登录页
- 创建家庭 / 加入家庭页
- 首页
- 衣橱列表页
- 新建 / 编辑衣橱页
- 衣物列表页
- 新增 / 编辑衣物页
- 衣物详情页
- 我的页

首页第一版可以保持简单：

- 显示当前家庭信息
- 显示衣橱入口
- 显示衣物管理入口

## 9. 联调核心流程

重点联调以下完整链路：

- 注册
- 登录
- 创建家庭
- 加入家庭
- 创建衣橱
- 添加衣物
- 查看衣物
- 编辑衣物
- 删除衣物

还要重点验证权限场景：

- 普通成员修改自己的衣物
- 普通成员修改别人的衣物
- 管理员修改他人的衣物

## 10. 真机测试与优化

联调完成后进行 App 真机测试，重点检查：

- 登录状态稳定性
- 页面跳转是否顺畅
- 图片上传是否可用
- 表单校验是否清晰
- 权限拦截是否正确

## 当前最先要做什么

现在第一步就是先完成数据库设计。

建议立刻按这个顺序开始：

1. 确定表结构
2. 创建 `uniCloud` 数据表
3. 再接 `uni-id-pages`

只有数据结构先定清楚，后面的家庭、衣橱和衣物开发才不会反复返工。

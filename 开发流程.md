# 家庭衣橱 App 第一版开发流程

## 开发目标

第一版先跑通这条核心业务链路：

`注册登录 -> 家庭引导（可跳过） -> 个人模式或家庭模式首页 -> 创建衣橱 -> 添加衣物 -> 查看与管理衣物`

## 当前已确认规则

- 一个用户最多加入一个家庭
- 用户不加入家庭，也可以直接使用个人模式
- 一个家庭可以有多个衣橱
- 个人空间也可以有多个衣橱
- 一件衣物可以不属于任何衣橱
- 家庭成员都可以创建衣物
- 个人模式下用户可以管理自己的衣物
- 家庭普通成员只能管理自己创建的衣物
- 家庭管理员可以管理家庭中所有成员创建的衣物
- 用户登录后，如果已加入家庭，直接进入首页
- 用户登录后，如果未加入家庭，进入家庭引导页，但可以跳过
- 创建家庭的人默认就是家庭管理员

## 开发原则

- 先定数据结构，再做页面和接口
- 先跑通最小业务闭环，再补交互细节
- 第一版只做核心功能，不提前扩展复杂能力
- 需求变更后，及时同步更新需求、流程、数据库、接口文档
- 每次实施前先说明本次目标、范围和验收方式
- 每次只做一个小批次任务，方便测试和验收

## 开发步骤

## 1. 明确数据模型

先确认第一版的核心对象和关系：

- 用户
- 家庭
- 家庭成员关系
- 衣橱
- 衣物

这一阶段重点确认：

- 哪些数据属于用户
- 哪些数据属于家庭
- 哪些数据同时支持个人模式和家庭模式
- 哪些数据允许为空，例如衣物的 `closet_id`
- 哪些操作受角色权限控制

## 2. 建立数据库表

在 `uniCloud` 中先建立第一版必需的数据表：

- `uni-id-users`
- `families`
- `family_members`
- `closets`
- `clothes`

同时确定：

- 字段名
- 字段类型
- 表间关联
- 索引
- 读写权限规则

## 3. 接入注册登录

使用 `uni-id-pages` 完成：

- 注册
- 登录
- 获取当前用户信息
- 退出登录

这一步完成后，应用要能识别当前用户身份，为后续家庭和衣物操作提供用户上下文。

当前登录页实现策略：

- 登录认证仍复用 `uni-id-co`
- 登录页前端展示已迁移到项目自定义页面
- 注册页前端展示已迁移到项目自定义页面
- 官方 `uni-id-pages` 登录页先保留，作为兜底和参考
- 首页当前已补充退出登录按钮，退出后回到自定义登录页

## 4. 开发家庭流程

先开发家庭相关的最小闭环：

- 创建家庭
- 生成家庭邀请码
- 通过邀请码加入家庭
- 查询当前用户所属家庭
- 查询家庭成员列表

同时需要保证：

- 一个用户只能加入一个家庭
- 已加入家庭的用户不能再次加入其他家庭
- 用户未加入家庭时可以跳过家庭流程，先进入个人模式
- 创建家庭的人自动成为管理员

### 注册登录如何处理

第一版不建议自己重写登录注册。

建议直接复用：

- `uni-id-pages` 的前端页面能力
- `uni-id-co` 的云对象能力
- `uni-id-users` 的用户表

这里的关系要明确：

- `uni-id-pages` 不是数据表
- `uni-id-pages` 是官方提供的一套登录注册页面和调用封装
- `uni-id-co` 是它配套的云对象
- `uni-id-users` 才是真正存用户数据的表

第一版建议只启用最简单的登录方式：

- 用户名密码注册
- 用户名密码登录

短信验证码、一键登录、第三方登录都先不开。

### 创建家庭如何处理

创建家庭建议不要直接由前端写数据库。

建议通过自定义云对象或云函数统一处理，原因是这里有明显的业务校验：

- 用户是否已加入家庭
- 邀请码是否唯一
- 创建家庭后是否自动写入成员表
- 创建人是否自动设为管理员

建议第一版优先设计以下接口：

- `createFamily`
- `joinFamilyByInviteCode`
- `getCurrentFamily`
- `getFamilyMembers`

其中：

- `createFamily` 负责创建 `families` 和 `family_members`
- `joinFamilyByInviteCode` 负责校验邀请码并写入成员关系
- `getCurrentFamily` 负责查询当前用户所属家庭
- `getFamilyMembers` 负责查询当前家庭成员列表

### 登录后的页面分流

登录成功后建议按下面顺序分流：

- 已加入家庭：直接进入首页
- 未加入家庭：进入家庭引导页
- 在家庭引导页中选择跳过：进入个人首页

当前实现策略：

- 使用统一入口页承接所有登录后回跳
- 入口页只负责分流，不承载业务界面
- 当前通过家庭云对象 `getCurrentFamily` 判断是否已加入家庭
- 登录后分流已切到服务端查询，后续可在此基础上继续补加入家庭等接口

## 当前是否要删除其他表和插件

不建议现在直接删除 `uni_modules` 里的插件资源文件和 schema 文件。

原因：

- 这些文件大多属于插件自带资源，不初始化数据库并不会影响当前业务
- 直接删除后，后续如果继续用 `uni-id-pages` 登录能力，容易破坏依赖
- 当前真正需要控制的是“初始化哪些表”，而不是“物理删除哪些插件文件”

所以当前推荐做法是：

- 不初始化不用的表
- 保留插件目录文件
- 仅初始化第一版真正需要的数据库表

## 5. 开发首页与作用域流程

在家庭流程完成后，先搭建首页的作用域能力：

- 支持个人模式首页
- 支持家庭模式首页
- 明确当前使用的是个人空间还是家庭空间

## 6. 开发衣橱流程

在个人或家庭维度下开发衣橱管理：

- 创建衣橱
- 查询衣橱列表
- 编辑衣橱
- 删除衣橱
- 选择预设衣柜样式
- 选择衣柜颜色
- 填写衣柜所属房间

这里要注意：

- 衣橱既可能归属于个人，也可能归属于家庭
- 一个家庭可以有多个衣橱
- 个人空间也可以有多个衣橱
- 删除衣橱时，要考虑已绑定该衣橱的衣物如何处理
- 一期衣橱先不接用户真实图片上传
- 一期衣橱先不接 3D 建模能力

第一版建议处理方式：

- 删除衣橱时，将关联衣物的 `closet_id` 置空

### 衣橱一期正式实现方向

一期衣橱建议先按“预设样式衣柜”实现，先跑通以下链路：

1. 衣橱主数据闭环

- 能创建衣橱
- 能查看衣橱列表
- 能查看衣橱详情
- 能编辑衣橱基础信息
- 能删除衣橱

当前已完成：

- 个人空间衣橱列表
- 个人空间新建衣橱
- 家庭空间衣橱列表
- 家庭空间新建衣橱
- 衣橱编辑
- 衣橱删除

2. 衣橱配置链路

- 能选择预设衣柜样式
- 能选择衣柜颜色
- 能填写衣柜名称
- 能填写或选择所属房间

3. 衣橱展示链路

- 衣橱列表页能展示样式缩略图、颜色、名称、房间
- 衣橱详情页能展示衣橱基础信息
- 后续衣物模块能基于 `closet_id` 绑定到衣橱

4. 远期预研保留项

- 衣柜 3D 形象能力当前不进入一期开发
- `腾讯混元生3D API` 仅保留为后续扩展预研方案

## 7. 开发衣物管理

这是第一版最核心的模块，优先完成：

- 添加衣物
- 查看衣物列表
- 查看衣物详情
- 编辑衣物
- 删除衣物
- 为衣物选择所属衣橱
- 支持衣物不绑定衣橱

同时实现基础筛选：

- 按衣橱筛选
- 按创建人筛选
- 按分类筛选
- 按季节筛选

## 8. 开发权限控制

在衣物和衣橱模块开发时，同步完成权限规则：

- 个人模式下用户只能读取和管理自己的数据
- 家庭普通成员只能编辑和删除自己创建的衣物
- 家庭管理员可以编辑和删除家庭内所有衣物
- 非本家庭成员不能读取家庭数据

建议：

- 简单读取可用 `clientDB`
- 涉及权限判断的创建、编辑、删除优先走云函数或云对象

## 9. 搭建页面结构

在业务接口基本明确后，搭建页面：

- 登录页
- 统一入口页
- 家庭引导页（创建家庭 / 加入家庭 / 跳过）
- 首页
- 衣橱列表页
- 新建 / 编辑衣橱页
- 衣物列表页
- 新增 / 编辑衣物页
- 衣物详情页
- 我的页

首页第一版可以保持简单：

- 显示当前空间是个人还是家庭
- 显示当前家庭信息（如处于家庭模式）
- 显示衣橱入口
- 显示衣物管理入口

页面开发约束：

- 页面拆分为页面容器 + 小组件
- 查询逻辑和分流逻辑放在独立服务模块
- 接口调用统一放在 `common/api` 层管理
- `common/api/modules` 只负责接口调用，不堆放页面流程判断
- `common/services` 负责状态装配、业务判断、路由编排
- 严禁把大量模板、样式和业务逻辑堆进单个大文件

## 10. 联调核心流程

重点联调以下完整链路：

- 注册
- 登录
- 家庭引导页跳过
- 创建家庭
- 加入家庭
- 创建衣橱
- 添加衣物
- 查看衣物
- 编辑衣物
- 删除衣物

还要重点验证权限场景：

- 个人模式下管理自己的衣物
- 家庭普通成员修改自己的衣物
- 家庭普通成员修改别人的衣物
- 家庭管理员修改他人的衣物

## 11. 真机测试与优化

联调完成后进行 App 真机测试，重点检查：

- 登录状态稳定性
- 页面跳转是否顺畅
- 图片上传是否可用
- 表单校验是否清晰
- 权限拦截是否正确

## 当前最先要做什么

现在第一步就是先完成文档修订和数据库设计修订。

建议立刻按这个顺序开始：

1. 同步更新四份核心文档
2. 确定双模式下的表结构
3. 再接登录后页面分流

只有数据结构和流程先定清楚，后面的家庭、衣橱和衣物开发才不会反复返工。

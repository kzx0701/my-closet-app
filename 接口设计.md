# 家庭衣橱 App 第一版接口设计

## 说明

第一版建议采用以下思路：

- 登录注册复用 `uni-id-pages` + `uni-id-co`
- 家庭、衣橱、衣物等业务接口由我们自己实现

这样可以减少重复造轮子，并把业务规则集中到自定义接口中控制。

## 登录注册部分

### 使用方案

- 前端页面：复用 `uni-id-pages`
- 云端登录对象：复用 `uni-id-co`
- 用户数据表：使用 `uni-id-users`

### 第一版启用方式

只启用：

- 用户名密码注册
- 用户名密码登录

不启用：

- 短信验证码登录
- 一键登录
- 第三方登录

### 结果

登录注册这部分第一版不需要你自己设计用户表接口。

只需要：

- 初始化 `uni-id-users`
- 配置 `uni-id-pages`
- 接入登录页和注册页

## 家庭模块接口

第一版建议优先实现以下接口。

### 1. `createFamily`

用途：

- 创建家庭
- 将当前用户写入成员表
- 自动设为管理员

输入建议：

- `name`

处理逻辑：

1. 校验当前用户是否已登录
2. 校验当前用户是否已加入家庭
3. 生成唯一 `invite_code`
4. 创建 `families`
5. 创建 `family_members`
6. 返回家庭信息

涉及表：

- `families`
- `family_members`

### 2. `joinFamilyByInviteCode`

用途：

- 用户通过邀请码加入家庭

输入建议：

- `invite_code`

处理逻辑：

1. 校验当前用户是否已登录
2. 校验当前用户是否已加入家庭
3. 根据邀请码查找家庭
4. 创建 `family_members`
5. 更新家庭成员数量
6. 返回家庭信息

涉及表：

- `families`
- `family_members`

### 3. `getCurrentFamily`

用途：

- 查询当前登录用户所属家庭

处理逻辑：

1. 根据 `auth.uid` 查 `family_members`
2. 查出对应 `families`
3. 返回家庭信息与当前角色

涉及表：

- `family_members`
- `families`

### 4. `getFamilyMembers`

用途：

- 查询当前家庭成员列表

处理逻辑：

1. 查当前用户所属家庭
2. 查询该家庭下的 `family_members`
3. 关联 `uni-id-users` 返回成员基本信息

涉及表：

- `family_members`
- `uni-id-users`

## 衣橱模块接口

第一版建议后续紧接着实现：

- `createCloset`
- `getClosetList`
- `updateCloset`
- `deleteCloset`

## 衣物模块接口

第一版建议后续实现：

- `createClothes`
- `getClothesList`
- `getClothesDetail`
- `updateClothes`
- `deleteClothes`

## 当前结论

第一版现在不应该先自己重写注册登录。

最合理的分工是：

- 用户注册登录：直接用 `uni-id-pages`
- 家庭相关：我们自己写云对象或云函数
- 衣橱相关：我们自己写云对象或云函数
- 衣物相关：我们自己写云对象或云函数

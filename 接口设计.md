# 家庭衣橱 App 第一版接口设计

## 说明

第一版建议采用以下思路：

- 登录注册复用 `uni-id-pages` + `uni-id-co`
- 家庭、衣橱、衣物等业务接口由我们自己实现
- 业务接口同时支持个人模式与家庭模式

这样可以减少重复造轮子，并把业务规则集中到自定义接口中控制。

## 登录注册部分

### 使用方案

- 前端页面：登录页、注册页使用项目自定义页面，找回密码暂不在当前版本开放入口
- 云端登录对象：复用 `uni-id-co`
- 用户数据表：使用 `uni-id-users`

### 第一版启用方式

只启用：

- 用户名密码注册
- 用户名密码登录

不启用：

- 短信验证码登录
- 一键登录
- 第三方登录

### 结果

登录注册这部分第一版不需要你自己设计用户表接口。

只需要：

- 初始化 `uni-id-users`
- 配置 `uni-id-pages`
- 接入登录页和注册页
- 复用 `uni-id-pages/common/store` 中的退出登录能力

### 登录后分流规则

- 已登录且已加入家庭：直接进入首页
- 已登录但未加入家庭：进入家庭引导页
- 家庭引导页支持“创建家庭”“加入家庭”“跳过”
- 跳过后进入首页，使用个人模式

当前实现：

- 使用统一入口页做登录后分流
- 已通过家庭云对象 `getCurrentFamily` 判断当前用户是否已加入家庭
- 前端页面不再直接读取 `family_members` 做分流

### 前端接口组织方式

- 页面层不直接访问 `uniCloud.database()`
- 查询和调用统一放在 `common/api` 目录
- 当前分层约定为：
  - `pages`：页面与组件
  - `common/services`：状态判断、数据装配、路由编排
  - `common/api/modules`：接口调用模块，只负责发起查询或调用
  - `common/api/clients`：底层客户端封装
- `common/api/modules` 中的接口文件需要带注释，明确每个接口的用途、输入和返回值

## 家庭模块接口

第一版建议优先实现以下接口。

### 1. `createFamily`

用途：

- 创建家庭
- 将当前用户写入成员表
- 自动设为管理员

输入建议：

- `name`

处理逻辑：

1. 校验当前用户是否已登录
2. 校验当前用户是否已加入家庭
3. 生成唯一 `invite_code`
4. 创建 `families`
5. 创建 `family_members`
6. 将创建者角色写为 `admin`
7. 返回家庭信息

涉及表：

- `families`
- `family_members`

当前实现说明：

- 已实现最小闭环
- 由家庭云对象 `family-co.createFamily` 执行
- 创建成功后自动插入 `family_members`
- 创建者角色固定写为 `admin`

### 2. `joinFamilyByInviteCode`

用途：

- 用户通过邀请码加入家庭

输入建议：

- `invite_code`

处理逻辑：

1. 校验当前用户是否已登录
2. 校验当前用户是否已加入家庭
3. 根据邀请码查找家庭
4. 创建 `family_members`
5. 更新家庭成员数量
6. 返回家庭信息

涉及表：

- `families`
- `family_members`

### 3. `getCurrentFamily`

用途：

- 查询当前登录用户所属家庭

处理逻辑：

1. 根据 `auth.uid` 查 `family_members`
2. 查出对应 `families`
3. 返回家庭信息与当前角色

涉及表：

- `family_members`
- `families`

当前实现说明：

- 已实现最小闭环
- 由家庭云对象 `family-co.getCurrentFamily` 执行
- 当前用于登录后分流与首页空间状态判断

### 4. `getFamilyMembers`

用途：

- 查询当前家庭成员列表

处理逻辑：

1. 查当前用户所属家庭
2. 查询该家庭下的 `family_members`
3. 关联 `uni-id-users` 返回成员基本信息

涉及表：

- `family_members`
- `uni-id-users`

## 衣橱模块接口

第一版建议后续紧接着实现：

- `createCloset`
- `getClosetList`
- `updateCloset`
- `deleteCloset`

说明：

- 衣橱接口需要支持 `personal` 和 `family` 两种作用域
- `family` 作用域下需要校验当前用户是否属于目标家庭
- `personal` 作用域下只允许操作自己的数据
- 一期衣橱接口需支持预设样式、颜色、房间字段
- 一期不接真实图片上传和 3D 建模

当前实现进度：

- 已完成 `createCloset`
- 已完成个人空间 `getClosetList`
- 已完成家庭空间 `getClosetList`
- 已完成 `getClosetDetail`
- 已完成 `updateCloset`
- 已完成 `deleteCloset`

### 衣橱一期字段方向

基础衣橱接口当前建议至少支持以下输入输出字段：

- `name`
- `room_name`
- `style_code`
- `color_code`
- `scope_type`
- `scope_owner_user_id`
- `family_id`

### 衣橱 3D 能力保留说明

- 衣柜 3D 形象能力当前不进入一期开发
- `腾讯混元生3D API` 保留为远期扩展预研方案

## 衣物模块接口

第一版建议后续实现：

- `createClothes`
- `getClothesList`
- `getClothesDetail`
- `updateClothes`
- `deleteClothes`

说明：

- 衣物接口需要支持 `personal` 和 `family` 两种作用域
- `personal` 作用域下仅本人可读写
- `family` 作用域下普通成员仅可管理自己创建的数据
- `family` 作用域下管理员可管理家庭内全部数据

## 当前结论

第一版现在不应该先自己重写注册登录。

最合理的分工是：

- 用户注册登录：直接用 `uni-id-pages`
- 家庭相关：我们自己写云对象或云函数
- 衣橱相关：我们自己写云对象或云函数
- 衣物相关：我们自己写云对象或云函数

## 文档维护要求

后续如果以下内容发生确认或变更，需要同步更新：

- `项目需求.md`
- `开发流程.md`
- `数据库设计.md`
- `接口设计.md`

同步场景包括：

- 需求变化
- 功能变化
- 页面流转变化
- 数据结构变化
- 接口策略变化
- 实现方式变化

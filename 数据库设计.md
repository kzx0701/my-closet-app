# 家庭衣橱 App 第一版数据库设计

## 设计目标

第一版数据库只服务于以下核心能力：

- 注册登录
- 创建家庭 / 加入家庭
- 个人模式下管理自己的衣橱和衣物
- 创建多个衣橱
- 添加和管理衣物
- 基于角色控制衣物管理权限

## 表清单

第一版建议使用以下数据表：

- `uni-id-users`
- `families`
- `family_members`
- `closets`
- `clothes`

## 模式说明

第一版不是“必须加入家庭后才能使用”的模型，而是双模式模型：

- `personal`：个人空间
- `family`：家庭空间

说明：

- 用户未加入家庭时，也可以在个人空间下使用衣橱和衣物功能
- 用户加入家庭后，同时拥有个人空间和家庭空间
- 第一版不做个人数据自动迁移到家庭

## 1. `uni-id-users`

说明：

- 用户基础表
- 直接复用 `uni-id-pages`
- 不建议第一版自建用户表替代它

第一版主要使用这些字段：

- `_id`
- `username`
- `nickname`
- `mobile`
- `avatar_file`

如有额外资料需求，建议后续新增扩展字段，而不是另建重复用户表。

## 2. `families`

说明：

- 家庭主表
- 一个家庭对应多名成员
- 一个家庭对应多个衣橱

建议字段：

- `_id`：家庭 ID
- `name`：家庭名称
- `invite_code`：家庭邀请码
- `owner_user_id`：家庭创建人用户 ID
- `status`：家庭状态，建议值 `active`
- `member_count`：成员数量
- `created_at`：创建时间
- `updated_at`：更新时间

建议索引：

- `invite_code`
- `owner_user_id`

## 3. `family_members`

说明：

- 家庭与用户关系表
- 用于约束一个用户只能属于一个家庭
- 用于记录家庭角色

建议字段：

- `_id`：关系记录 ID
- `family_id`：家庭 ID
- `user_id`：用户 ID
- `role`：角色，建议值 `admin` / `member`
- `status`：状态，建议值 `active`
- `joined_at`：加入时间
- `created_at`：创建时间
- `updated_at`：更新时间

建议索引：

- `family_id`
- `user_id`
- `family_id + user_id` 组合唯一索引
- `user_id` 唯一索引

说明：

- `user_id` 唯一索引用于保证“一个用户只能加入一个家庭”
- 创建家庭的人默认写入本表，角色固定为 `admin`

## 4. `closets`

说明：

- 衣橱表
- 支持归属于个人空间或家庭空间
- 一个家庭可以有多个衣橱
- 一个用户的个人空间也可以有多个衣橱

建议字段：

- `_id`：衣橱 ID
- `scope_type`：作用域类型，建议值 `personal` / `family`
- `scope_owner_user_id`：个人空间归属用户 ID，个人模式必填
- `family_id`：所属家庭 ID，家庭模式必填
- `name`：衣橱名称
- `description`：衣橱描述
- `created_by`：创建人用户 ID
- `status`：状态，建议值 `active`
- `sort`：排序值
- `created_at`：创建时间
- `updated_at`：更新时间

建议索引：

- `scope_type`
- `scope_owner_user_id`
- `family_id`
- `created_by`
- `scope_type + scope_owner_user_id`
- `scope_type + family_id`

## 5. `clothes`

说明：

- 衣物主表
- 衣物可以属于个人空间或家庭空间
- 衣物可以不属于任何衣橱
- 个人模式下仅本人可管理
- 家庭模式下普通成员只能管理自己创建的衣物

建议字段：

- `_id`：衣物 ID
- `scope_type`：作用域类型，建议值 `personal` / `family`
- `scope_owner_user_id`：个人空间归属用户 ID，个人模式必填
- `family_id`：所属家庭 ID，家庭模式必填
- `closet_id`：所属衣橱 ID，可为空
- `created_by`：创建人用户 ID
- `name`：衣物名称
- `category`：分类
- `season`：季节
- `color`：颜色
- `image_url`：图片地址
- `remark`：备注
- `status`：状态，建议值 `active`
- `created_at`：创建时间
- `updated_at`：更新时间

建议索引：

- `scope_type`
- `scope_owner_user_id`
- `family_id`
- `closet_id`
- `created_by`
- `scope_type + scope_owner_user_id`
- `scope_type + family_id`
- `scope_type + family_id + created_by`

## 字段命名建议

建议统一采用下划线风格命名：

- `scope_type`
- `scope_owner_user_id`
- `family_id`
- `user_id`
- `closet_id`
- `created_by`
- `created_at`
- `updated_at`

这样后续在云函数、前端表单和权限规则里会更统一。

## 第一版分类字段建议

`clothes.category` 可先固定为：

- `top`
- `bottom`
- `outerwear`
- `shoes`
- `accessory`

`clothes.season` 可先固定为：

- `spring`
- `summer`
- `autumn`
- `winter`

## 第一版权限落点

数据库结构主要承担“归属关系”记录，权限判断建议这样做：

- 个人模式通过 `scope_owner_user_id` 判断是否为数据所有者
- 通过 `family_members.role` 判断是否管理员
- 通过 `clothes.created_by` 判断是否为衣物创建人
- 通过 `family_id` 校验用户是否属于当前家庭（家庭模式）

第一版建议重点在云函数或云对象里处理这些权限逻辑，不建议完全依赖前端控制。

## 当前结论

现在初步需要建立的表就是这 5 张：

- `uni-id-users`
- `families`
- `family_members`
- `closets`
- `clothes`

如果后续需求增加，例如衣物多图、衣物标签、衣橱共享策略、操作日志，再继续拆分新表会更合适。

## 当前规则补充

- 用户未加入家庭时，可以直接使用个人空间
- 用户创建家庭后，自动成为该家庭管理员
- 家庭空间用于多人协作，个人空间用于个人独立管理
- 当前家庭最小闭环已按 `families + family_members` 落地
- `createFamily` 会同时写入家庭主表和成员关系表
- `getCurrentFamily` 当前作为登录后分流和家庭状态识别的正式查询入口

## 第一版初始化建议

除了上面的业务表，项目里还存在一些来自 `uni-id-pages`、`uni-captcha`、`uni-open-bridge-common`、`uni-upgrade-center` 的配套表。

第一版建议按下面分类处理。

### 必须初始化

- `uni-id-users`
- `families`
- `family_members`
- `closets`
- `clothes`

说明：

- `uni-id-users` 是登录注册必须使用的用户主表
- 其余 4 张表是当前家庭衣橱业务必需表

### 建议初始化

- `uni-id-roles`
- `uni-id-permissions`

说明：

- 如果你后续准备继续沿用 `uni-id` 的角色权限体系，这两张表建议一开始就初始化
- 即使第一版家庭管理员和普通成员的业务权限主要放在 `family_members.role` 中处理，这两张表保留也更稳妥

### 当前可以不初始化

- `uni-id-device`
- `uni-id-log`
- `opendb-device`
- `opendb-tempdata`
- `opendb-frv-logs`
- `opendb-verify-codes`
- `opendb-open-data`
- `opendb-app-list`
- `opendb-app-versions`

说明：

- `uni-id-device`、`opendb-device`：主要用于设备、推送等能力，第一版不做可先不建
- `uni-id-log`：主要用于登录行为日志，第一版不是必需
- `opendb-tempdata`：通常用于临时数据能力，第一版不需要
- `opendb-frv-logs`：用于实名或刷脸相关能力，第一版不需要
- `opendb-verify-codes`：主要用于验证码插件，若第一版不用验证码可不建
- `opendb-open-data`：用于开放桥接相关能力，当前项目不需要
- `opendb-app-list`、`opendb-app-versions`：用于升级中心，当前业务不需要

### 初始化顺序建议

建议第一版先按下面顺序初始化：

1. `uni-id-users`
2. `uni-id-roles`
3. `uni-id-permissions`
4. `families`
5. `family_members`
6. `closets`
7. `clothes`

如果你想把第一版建库做到最精简，也可以先初始化：

- `uni-id-users`
- `families`
- `family_members`
- `closets`
- `clothes`

后面等登录、角色或验证码能力真正用到时，再补其他表。
